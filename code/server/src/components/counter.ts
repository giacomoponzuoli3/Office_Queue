/**
 * Represents a counter of the office
 */
class Counter {
    id: number;
    name: String;

    constructor(id: number, name: string) {
        this.id = id;
        this.name = name;
    }
}

export { Counter}